import 'package:flutter/material.dart';

class RegestirScreen extends StatelessWidget {
  const RegestirScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
